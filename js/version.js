// Bumped by hand on every deploy-worthy change, shown in Evia's footer so a
// phone session can be checked against what's actually live (vs. a stale
// cached copy) without guessing.
export const VERSION = '2026-09-24.3';
